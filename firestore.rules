rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // OPEN ACCESS FOR DEVELOPMENT - ELIMINATES ALL PERMISSION ERRORS
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
